const theme = {
  // Change these two per client = completely different look
  primary: "#C8A96E",    // gold accent
  secondary: "#1A1A2E",  // deep navy

  // Font pairing (loaded from Google Fonts in layout.js later)
  headingFont: "Playfair Display", // elegant, hotel-feel
  bodyFont: "Inter",               // clean, readable
};

export default theme;